library pocket_client.enums;

enum DetailType { simple, complete }

enum ContentType { article, video, image }

enum SortType { newest, oldest, title, site }

enum State { unread, archive, all }

enum Status { normal, archived, toBeDeleted }
